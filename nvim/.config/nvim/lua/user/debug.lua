local symbols = require("symbols-outline.providers.jsx")
local utils = require"user.utils"
local buffer = require"user.utils.buffer"

return function ()
  -- print(buffer.buffer_max_line(0))
  -- local info = symbols.request_symbols()
  -- print(utils.dump(info))
end
